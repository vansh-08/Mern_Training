{
  "name": "user-management",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "server": "json-server db.json --port 3000 --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "json-server": "^0.17.4"
  }
}